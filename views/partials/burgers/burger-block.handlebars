<li>
	<h3 class="text-center">{{burger_name}}</h3>
		{{#if devoured}}
			<button class="devoured center-block" data-id="{{id}}" data-devour="{{devoured}}" disabled>Already Eaten...</button>
		{{else}}
			<button class="devoured center-block" data-id="{{id}}" data-devour="{{devoured}}">Eat Me!</button>
		{{/if}}
</li>
